# DevOps
1- Run and connect both applications using a Docker network.

2- Connect both applications using Docker Compose.

3- Install a lightweight distribution of Kubernetes (K3s).

4- Install a mini Kubernetes cluster using helpers/tools, Kind (Kubernetes in Docker), and Minikube (a single-node Kubernetes cluster that runs in a VM or container).
